# Secrets & MCP config (API keys must never be committed)
.mcp.json
.env
.env.local

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.venv/
venv/

# Trading runtime state (changes every session — not for VCS)
trading/open_positions.json
trading/trade_log.json
trading/logs/*.log
logs/
strategies/session_state.json

# Cached/regenerable backtest data (re-fetched by tools/fetch_data.py)
tools/data/

# Dashboard build artifacts
dashboard/node_modules/
dashboard/.next/
dashboard/.vercel/
dashboard/.env.local

# Claude Code local settings (memory consolidated into the global auto-memory
# folder 2026-08-09 — repo-local .claude/memory/ retired, nothing to except anymore)
.claude/*

# OS
.DS_Store
Thumbs.db
